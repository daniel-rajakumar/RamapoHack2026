Place MediaPipe runtime assets here.

Required files:
- /public/mediapipe/wasm/*
- /public/mediapipe/hand_landmarker.task

Run:
- npm run setup:mediapipe

The app will try local assets first, then fall back to CDN if local assets are missing.
