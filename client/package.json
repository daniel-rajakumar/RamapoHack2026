{
  "name": "gesture-shooter-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "setup:mediapipe": "node ./scripts/setup-mediapipe.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.20",
    "socket.io-client": "^4.8.1",
    "three": "^0.173.0"
  },
  "devDependencies": {
    "@types/three": "^0.173.0",
    "playwright": "^1.51.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
